version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./output:/app/output
      - ./settings.txt:/app/settings.txt
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
